# Havoc

A browser chaos-engineering tool for injecting controlled failures and uncovering how applications recover.

## Project Structure

    havoc/
      extension/   # the MV3 extension (TypeScript + Svelte + Vite)
      .gitignore
      README.md
      LICENSE
      package.json

## Setup

    cd extension
    npm install

## Build

    npm run build
